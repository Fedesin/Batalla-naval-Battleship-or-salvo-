namespace Salvo.Models
{
    public enum GameState
    {
        ENTER_SALVO = 0,
        PLACE_SHIPS,
        WAIT,
        WIN,
        LOSS,
        TIE,
        WAIT_PLAYER,
        WAIT_PLAYER_SHIPS,
        PLAYING,
        FINISH
    }
}
