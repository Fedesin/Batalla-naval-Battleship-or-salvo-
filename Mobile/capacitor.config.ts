import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tech.g2csolutions.salvo',
  appName: 'Salvo G2C',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
